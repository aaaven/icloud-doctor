interface:
  display_name: "iCloud Doctor"
  short_description: "Diagnose multi-Mac iCloud sync and recovery issues"
  default_prompt: "Use $icloud-doctor workspace-check to verify that the current authorized workspace is synchronized and locally ready before work begins."
